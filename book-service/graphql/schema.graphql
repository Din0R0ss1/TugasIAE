type Book {
    id: ID!
    judul: String!
    penulis: String!
    stok: Int!
    created_at: String
    updated_at: String
}

type Query {
    books: [Book!]! @all
    book(id: ID! @eq): Book @find
}
